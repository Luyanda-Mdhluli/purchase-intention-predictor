# Pinned. random_forest_model.pkl is pickled with scikit-learn 1.9.0; loading it under
# a different version is not guaranteed to give the same predictions, and app.py warns
# at startup if they do not match. To move to a newer scikit-learn, bump the pin, rerun
# train.py, and commit the regenerated model.
scikit-learn==1.9.0
numpy==2.5.2
pandas==3.0.5
joblib==1.6.0
streamlit==1.63.0
plotly==7.0.0
# Reading the .xlsx — needed by train.py only, not by the app.
openpyxl==3.1.5
