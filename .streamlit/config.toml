# Theme lives here rather than in injected CSS: this is the supported API and it
# survives Streamlit DOM changes, and it keeps text readable when the viewer's
# browser is in dark mode.
[theme]
base = "light"
primaryColor = "#b38600"
backgroundColor = "#fff9e6"
secondaryBackgroundColor = "#fff3cc"
textColor = "#000000"
